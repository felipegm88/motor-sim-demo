name: clang-format

on:
  pull_request:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install clang-format
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends clang-format

      - name: Check formatting
        run: |
          # Adjust extensions if you add more
          FILES=$(git ls-files '*.c' '*.h')
          if [ -z "$FILES" ]; then
            echo "No C/H files found."
            exit 0
          fi

          # Fail if clang-format would change anything
          clang-format -style=file -Werror --dry-run $FILES

